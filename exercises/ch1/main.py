import math
import random

def area_rect(width, length):
    """
    Given 2 inputs, width and lenght representing a rectangle,
    calculate the area of such rectangle
    """
    return width * length

def convert_to_c(degree_in_f):
    """
    Given input as degree in fahrenheit, 
    write a one line python expression to return a degree in celsius
    """
    return 0

def three_average(num1, num2, num3):
    """
    Calculate the average of the 3 input numbers
    """
    return 0

def divide_cookies(num_cookies, num_kids):
    """
    Given 2 inputs, a number of cookies and a total number
    of kids, calculate how many WHOLE cookies can each
    kid have.  
    
    i.e. if I have 3 cookies and 2 kids, each kid
    will only have 1 cookie since we are only interested
    in whole cookies.
    """
    return 0

def remaining_cookies(num_cookies, num_kids):
    """
    Continuing from the previous question, I want to 
    find out how many cookies remain after
    dividing all the cookies betwen kids.
    """
    return 0

def runway(bearing):
    """
    Airport runways are given numbers based on the direction 
    planes travel as they move along the runway.
    
    The number is determined by taking the bearing, in degrees, 
    and rounding to the nearest 10 degrees and then dropping the last zero.
    
    E.g., the number for a runway with bearing 267 degrees would be: 
    (a) round 267 to 270
    (b) discard last zero, to get: 27
    
    Given a bearing as input (an integer between 0 to 360) returns the corresponding runway number.
    """
    return 0


def distance_from_origin(x, y):        
    """
    Write a function to compute the distance of an (x,y) 
    point from the origin on a 2D plane.  
    HINT: you can use math.sqrt( number ) to return the square root of a number
    """
    return 0

def distance_between_points(x1, y1, x2, y2):
    """
    Write a function to compute the distance between 
    two points (x1, y1) and (x2, y2) on a 2D plane.
    """
    return 0

def intersect_circle(x1, y1, r1, x2, y2, r2):
    """
    Write a function to determine if two circles 
    (x1, y1, r1), and (x2, y2, r2) on a 2D plane are intersecting.
    """
    return 0

def random_integer():
    """
    Write a function that returns a random integer between 1 to 6
    """
    return 0

def die(sides):
    """
    Write a function that take, as input, the number of sides a die has, and return a simulated roll that that die.
    For example, die(20) would return a simulated result from a 20-sided die.
    """
    return 0

def pair_die():
    """
    Write a function that returns the result of a pair of 20-sided dice.
    """
    return 0
    
    
